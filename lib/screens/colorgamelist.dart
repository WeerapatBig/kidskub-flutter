import 'package:flutter/material.dart';

class ColorGameList extends StatelessWidget {
  const ColorGameList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
