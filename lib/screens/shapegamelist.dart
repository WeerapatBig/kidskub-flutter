import 'package:flutter/material.dart';

class ShapeGameList extends StatelessWidget {
  const ShapeGameList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
